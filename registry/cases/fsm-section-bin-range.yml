# Free-space section size selects a bin past the manager's bin array.
#
# This is the investigated, oracle-hardened counterpart to the two open items in
# fsm-section-divergences.yml. Those asked whether libhdf5 merely *reports* an
# out-of-file free section or actually *uses* it. The answer here is stronger and
# concerns a different field: the section SIZE is used as an array index, so a
# malformed size is a memory-safety defect, not only a reporting gap.
schema_version: 1

id: fsm-section-bin-range
title: free-space section size indexes sinfo->bins unchecked
status: oracle-hardened          # h5policy rejects; libhdf5 containment still planned
disclosure: "OSS-Fuzz 42521342, 42523993; advisory text not retrieved -- local
  evidence only. Version range below is what this host measured."

record: validation_controls
root_cause_classification: aggregate_object
violated_invariant: control.fsm_section_bin_range
entry_points:
  - h5policy (oracle) -- untrusted-strict, now rejects
  - "libhdf5 H5Fget_free_sections -> H5MF_get_free_sections -> H5FS_sect_iterate
     -> H5FS__sinfo_lock -> H5AC_protect -> H5FS__cache_sinfo_deserialize
     -> H5FS_sect_add -> H5FS__sect_link -> H5FS__sect_link_size"
  - "shipped tools: h5stat -s, h5stat -F. NOT reached by H5Fopen + object walk
     (h5dump -H completes)."

# ── The defect, read out of the source tree (HDF5 2.3.0 @4b1ffa68a84) ─────────
defect: >-
  H5FS__sinfo_new sizes sinfo->bins as H5VM_log2_gen(max_sect_size) entries
  (H5FSsection.c:129,142), reading max_sect_size from the FSHD header.
  H5FS__sect_link_size indexes that array with bin = H5VM_log2_gen(sect->size)
  (H5FSsection.c:933) from an FSSE section record, guarded only by
  assert(bin < sinfo->nbins) at :934 -- compiled out under NDEBUG. A section
  whose size selects bin >= nbins reads off the end at :935 and writes off it at
  :936 and :969. H5FS__cache_sinfo_deserialize (H5FScache.c:999) decodes
  sect_size and validates only the section TYPE, never the size. The bound is
  relational to the header, so it is an aggregate-object invariant: neither the
  header nor the section record alone establishes it.
  Three further unguarded sinfo->bins[bin] sites share the pattern:
  H5FSsection.c:763, :1609 (indexed by an allocation request, not file data),
  and :1981.

boundary_note: >-
  nbins = H5VM_log2_gen(max_sect_size) is itself off by one: a section of size
  exactly max_sect_size yields bin == nbins, already one past the last slot. So
  the safe boundary the oracle enforces is log2(sect_size) < log2(max_sect_size),
  i.e. strictly less. A libhdf5 fix must decide whether to reject a maximal
  section or widen nbins to log2+1; the fractal-heap manager (max_sect_size =
  max_direct_size, H5HFspace.c:112) is where a maximal section could plausibly
  arise on a VALID file, and is not yet audited.

# ── Measured behaviour ────────────────────────────────────────────────────────
# Variant matrix: variants-matrix.json in the OSS-Fuzz 42523993 working bundle
# (untracked).
measured:
  oracle_before: >-
    accepted the three in-file neighbours variant_bin_edge / variant_bin_over /
    variant_specimen_size_in_file -- a soundness gap, because it checked only the
    section extent against the file, never the size against the bin array.
  oracle_after: >-
    rejects all three with H5_CORRUPT_FSM_SECTION_SIZE_OVER_MAX, while still
    accepting the in-range controls variant_fit / variant_bin_last /
    variant_header_permits (same 65535-byte section as a rejected row, but its
    header declares max_sect_size = 65536, so bin < nbins).
  libhdf5_stock: >-
    heap-layout dependent: the out-of-bounds access happens deterministically but
    does not always fault. The C probe SIGSEGVs on 42521342 and h5stat -s SIGSEGVs
    on 42523993; each survives the other. On the tracked fixture (bin == nbins,
    one slot past) stock silently reports the section.
  libhdf5_asan: >-
    both specimens and the tracked fixture fault identically -- heap-buffer-overflow
    READ of size 8 in H5FS__sect_link_size (H5FSsection.c:935), bytes past end ==
    32*(bin - nbins) + 24, a direct function of the encoded section size.

# ── Containment vs systemic remediation (strategy doc §11.5) ──────────────────
containment:
  status: planned
  description: >-
    In H5FS__cache_sinfo_deserialize, reject sect_size >= ((hsize_t)1 <<
    sinfo->nbins) before H5FS_sect_add; promote the assert at H5FSsection.c:934
    to HGOTO_ERROR. Backportable; no effect on well-formed files.
systemic:
  semantic_boundary: >-
    the free-space manager aggregate -- FSHD header plus the whole FSSE section
    list validated as one object before any section is linked into native state.
  status: planned
  owner: n/a

# ── Reproducers ───────────────────────────────────────────────────────────────
reproducers:
  direct_fixture: h5policy/tests/malformed/bad_fsm_section_bin_range.h5
  generator: "h5policy-gencorpus _make_bad_fsm_section_bin_range (shrinks
    max_sect_size to 4096, re-encodes one section of size 4096, extent in-file)"
  expectation: h5policy/tests/expected/malformed-bad_fsm_section_bin_range.yml
  integration_pocs:
    # Bundle paths added 2026-08-15.  These were previously named only by
    # OSS-Fuzz number, which made the record undiscoverable from the bundles and
    # left both of them sitting at `status: draft` as if unpromoted.  cases/ is
    # gitignored, so this record is the only tracked artifact for either.
    - specimen: OSS-Fuzz 42521342; input_sha256
        e5ac025e668dd38c5317d2a6856093320d9ef62bbbecfa073806fdc0b69a1cf4
      shape: bin 23 vs nbins 16; size field wild, address legal
      stock: the C probe SIGSEGVs on this one
    - specimen: OSS-Fuzz 42523993; input_sha256
        19cbef199143a4a240efce6d750a049be4429bffa089cf086122bca879507776
      shape: bin 15 vs nbins 12; both size and address wild
      stock: >-
        h5stat -s SIGSEGVs on this one; the dataset-layout probe reports
        accepted, and ASan confirms the over-read
        (comparison: confirmed_by_sanitizer).  Each specimen survives the
        other's trigger -- see measured.libhdf5_stock.
      variants_matrix: variants-matrix.json lives in this bundle
  fsm_targeted_probe: fsm-targeted-probe.c in each case bundle

sibling_audit: >-
  Two shapes share the defect. (1) The fractal-heap FSM (H5HFspace.c:112,
  max_sect_size = max_direct_size) has a small nbins for well-formed files, so
  the same unchecked sect_size reaches the same H5FS__sect_link_size -- combined
  with the off-by-one boundary this is a latent-on-valid-files risk if a writer
  can emit a max-sized heap section. (2) File FSMs (H5MF.c:397,
  max_sect_size = maxaddr) have nbins ~ 63 and hide the bug, which is why the
  fuzzer had to shrink max_sect_size in the header to reach it.

# ── Follow-up: re-verified on a newer build ───────────────────────────────────
followup_2026_09_04: >-
  Re-verified against a newer source revision (44426bfc7d5, three weeks past
  the 4b1ffa68a84 pin above) while drafting a private advisory for the
  OSS-Fuzz 42523993 specimen (input_sha256 19cbef19...). UNCHANGED: every
  line cited in `defect` still matches verbatim, oracle_after's mapping still
  resolves automatically (h5cve triage needs no manual intervention), and
  libhdf5_stock/libhdf5_asan still reproduce exactly as recorded -- h5stat -s
  SIGSEGVs on a stock baseline build, and an ASan candidate built WITH
  -DNDEBUG reports the identical heap-buffer-overflow READ of size 8 at
  H5FSsection.c:935, with bytes-past-end == 32*(bin-nbins)+24 == 120 for this
  specimen's bin=15/nbins=12 -- matching this record's own formula exactly.
  Both containment and systemic remain planned; no fix has landed upstream.
  ONE REFINEMENT the original record did not distinguish: libhdf5_asan above
  says an ASan build faults "identically" on all three specimens, but that is
  only true of an ASan build compiled WITHOUT -DNDEBUG's usual companion of
  disabling asserts. Confirmed this pass: an ASan candidate that also keeps
  asserts LIVE aborts on assert(bin < sinfo->nbins) at H5FSsection.c:934
  before the sanitizer gets a chance to report anything -- the assert masks
  the memory-unsafety witness on that build. Only an ASan build with asserts
  compiled out (-DNDEBUG, matching how HDF5 is normally shipped) reaches and
  reports the heap-buffer-overflow. Worth recording because "the ASan build
  faults" is two different builds with two different outcomes, and only one
  of them is the shipped-configuration witness.
  UPDATE, same day: the sibling specimen (OSS-Fuzz 42521342, input_sha256
  e5ac025e...) was re-verified too, for its own private advisory draft. Same
  outcome as above -- unchanged lines, oracle maps automatically, ASan with
  -DNDEBUG reports the identical heap-buffer-overflow shape at this
  specimen's own bin=23/nbins=16 (bytes-past-end == 32*(23-16)+24 == 248,
  past a 520-byte allocation -- matching this record's stated shape for it),
  and the assert-masks-asan-with-live-asserts refinement above holds here
  too.
  ONE MORE THING THIS PASS ADDED: `libhdf5_stock` above says both specimens'
  stock behaviour is "heap-layout dependent... does not always fault", and
  names only two outcomes (42521342's C-probe SIGSEGV, 42523993's h5stat -s
  SIGSEGV). There is a THIRD outcome, found running h5stat -s on 42521342
  specifically (not previously characterized -- the tracked shape only names
  what SIGSEGVs, not what does not): it does NOT crash. Instead it prints
  `HDF5: infinite loop closing library` (src/H5.c:466, the H5_term_library
  shutdown loop giving up after 100 tries with the free-list `FL` interface
  still pending) and reports internally inconsistent free-space statistics --
  one code path counts 1 small section, the bin-array counter this defect's
  out-of-bounds increment targets (H5FSsection.c:969) reports 0 total
  sections. A known-valid control file (h5policy/tests/valid/empty.h5)
  produces neither symptom under the same command, and the specimen
  reproduces this identically across repeated runs. `sinfo->bins` is itself
  allocated through the same free-list subsystem (H5FL_seq_calloc) that then
  reports pending on shutdown, which is a plausible, concrete mechanism --
  not merely a coincidence -- linking this non-crash symptom to the same
  out-of-bounds write the ASan report already locates. So the exact-build
  probe crashing on 42521342 while h5stat -s on the SAME specimen and library
  produces corrupted-but-live output instead is a fourth data point for
  "heap-layout dependent", not a discrepancy: which of (clean crash,
  corrupted-but-live, clean accept) a given consumer sees on a given
  specimen depends on that process's own prior allocation history, and all
  three have now been observed across the two specimens.

coverage_manifest_change: >-
  validation-coverage.yml validation_controls gains control.fsm_section_bin_range;
  findings/catalog/validation_controls.yml gains H5_CORRUPT_FSM_SECTION_SIZE_OVER_MAX
  (scope aggregate_object, non-shared, so no route rule).

provenance:
  found_by: "OSS-Fuzz specimens 42521342 and 42523993; worked end to end 2026-07-23"
  promoted_bundles:
    # Both were promoted BY this record on 2026-07-23; only the back-references
    # were missing, and both bundles were marked promoted on 2026-08-15.  Do not
    # re-promote them -- they are covered here, not open.
    - the OSS-Fuzz 42521342 working bundle (input_sha256 e5ac025e...)
    - the OSS-Fuzz 42523993 working bundle (input_sha256 19cbef19...)
  hdf5_build: "HDF5 2.3.0 @4b1ffa68a84; baseline Release, candidate RelWithDebInfo
    -fsanitize=address. Both are workstation-local installs; paths not recorded."
