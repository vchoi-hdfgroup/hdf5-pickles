# Message routing for H5_CORRUPT_V2_BTREE_ROOT_COUNT.
# Authoritative source; the loader expands each matches list and applies
# longest-substring-first precedence.
schema_version: 1
finding: H5_CORRUPT_V2_BTREE_ROOT_COUNT

routes:
  # The loader orders matches LONGEST FIRST -- substring matching, first hit wins.
  # One shared v2-btree engine serves every role, so the family comes from
  # the message; the invariant is that family's own, because `record`
  # selects the exact-build canary.
  - record: chunk_index
    invariant: chunk.btree_root_count
    scope: aggregate_object
    evidence: curated
    matches:
      - "leaf-root v2 B-tree total record count differs from its root count"
      - "chunk-index v2 B-tree total record count differs from its node graph"
      - "v2 B-tree has records but no root node"
  - record: shared_messages_legacy
    invariant: sohm.btree_root_count
    scope: aggregate_object
    evidence: curated
    matches:
      - "SOHM"
