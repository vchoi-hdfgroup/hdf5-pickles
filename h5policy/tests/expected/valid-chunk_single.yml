# One chunk covers this entire fixed-shape dataset, selecting the v4 single-
# chunk index.  Its layout stores the chunk address directly, so acceptance does
# not depend on a separate index structure.
file: valid/chunk_single.h5
profile: untrusted-strict
expected_decision: accept
expected_exit: 0
required_findings: []
h5cve:
  family: dataspace_dimension
  allowed_statuses: [verified]
verification:
  # The exact-fit control: one chunk covers the extent with nothing left over.
  count_and_extent_boundaries: [n]
forbidden: [crash, timeout, external_open, plugin_load, write]
