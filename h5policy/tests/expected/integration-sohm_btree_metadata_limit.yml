# The reduced ceiling permits the SOHM table, heap, B-tree header, and root,
# then saturates while charging the first complete fixed-size child node.
file: valid/sohm_btree.h5
profile: untrusted-strict
profile_overrides:
  resources:
    max_accounted_metadata_bytes: 1600
expected_decision: reject_resource
expected_exit: 4
required_findings: [H5_RESOURCE_ACCOUNTED_METADATA_BYTES]
expected_metrics:
  metadata_bytes_seen: 1600
h5cve:
  family: shared_messages_legacy
  # MEASURED 2026-09-02: the family exercise does not reach a SOHM surface on
  # this valid file, so `unexercised` is the honest status.
  allowed_statuses: [unexercised]
verification:
  integer_overflow_and_allocation_budget: [allocation_budget]
forbidden: [crash, timeout, external_open, plugin_load, write]
